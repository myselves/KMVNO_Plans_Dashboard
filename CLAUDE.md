# 저장소 규칙 (주간 자동 갱신 세션용)

이 저장소는 매주 금요일 13:00 KST에 Claude Code 루틴이 자동 갱신한다.

- 대시보드 원본은 `MVNO_요금제_비교_대시보드.html` 하나다. 반드시
  `mvno-dashboard-update` 스킬 절차(1~6단계 + 검증)로만 갱신한다.
- 갱신·검증 통과 후 `cp MVNO_요금제_비교_대시보드.html index.html`로
  배포용 사본을 동기화한다. 두 파일은 항상 동일 내용이어야 한다.
- `*.bak` 백업 파일은 커밋하지 않는다 (.gitignore 처리됨).
- 커밋은 저장소 기본 브랜치에 직접 하고 `git push -u origin HEAD`로
  푸시한다. PR은 만들지 않는다.
- 푸시하면 Vercel이 자동 배포한다. 별도 배포 작업은 하지 않는다.
- 수집 사이트 접근이 네트워크 정책으로 차단(403 CONNECT 거부)되어 있으면
  데이터를 갱신하지 말고, 기존 파일을 그대로 둔 채 실패 사유만 보고한다.
- README.md·CLAUDE.md·.gitignore는 사용자 요청 없이 수정하지 않는다.
